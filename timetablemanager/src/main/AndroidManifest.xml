<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cz.anty.timetablemanager">

    <uses-permission android:name="ANDROID.PERMISSION.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_scheme">
        <activity
            android:name=".TimetableSelectActivity"
            android:label="@string/activity_title_timetable_select" />
        <activity
            android:name=".TimetableManageActivity"
            android:label="@string/activity_title_timetable_manage" />

        <receiver
            android:name=".receiver.AttendanceReceiver"
            android:enabled="true"
            android:exported="false" />
        <receiver
            android:name=".receiver.TimetableScheduleReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>
        <receiver
            android:name=".widget.TimetableLessonWidget"
            android:label="@string/activity_title_timetable_manage">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/timetable_lesson_widget_info" />
        </receiver>

        <activity
            android:name=".widget.TimetableLessonWidgetConfigureActivity"
            android:label="@string/activity_title_timetable_select"
            android:theme="@style/AppTheme.Dialog">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>
    </application>

</manifest>
