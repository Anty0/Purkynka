package cz.anty.utils.attendance;

import junit.framework.TestCase;

/**
 * Created by anty on 18.9.15.
 *
 * @author anty
 */
public class TrackingMansTest extends TestCase {

    public void testToString() throws Exception {
        /*List<Man> mans = new ArrayList<>();
        Man testMan = new Man("TEST", "TEST_CLASS", new Date(System.currentTimeMillis()), Man.IsInSchoolState.IN_SCHOOL);
        mans.add(testMan);
        Gson gson = new Gson();
        String mansString = gson.toJson(mans.toArray(new Man[mans.size()]));
        Log.d("TrackingMansTest", "TestToString: " + mansString);
        Man[] resultMans = gson.fromJson(mansString, Man[].class);
        Log.d("TrackingMansTest", "TestToString equals: " + testMan.equals(resultMans[0]));*/
    }
}
